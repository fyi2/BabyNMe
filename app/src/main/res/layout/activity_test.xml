<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_standard"
    tools:context="org.sherman.android.babynme.Activites.TestActivity">

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fabGoHomeTest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:layout_marginEnd="16dp"
        android:clickable="true"
        android:onClick="home"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:rippleColor="@color/colorBlue"
        app:srcCompat="@drawable/ic_home_black_24dp" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/btnResetSharedPreferences"
            android:text="Reset Shared Preferences"
            android:clickable="true"
            android:onClick="resetSharedPreferences"
            />
    </RelativeLayout>
</android.support.constraint.ConstraintLayout>
